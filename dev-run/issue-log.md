# Issue Log

## ISSUE-A2-001 检索上下文增强

- 状态：Implemented
- 背景：
  - `docs/retrieval-pipeline-gap-analysis.md` 判断当前最大差距主要在 chunk 上下文不足与 answer 证据窗口偏窄。
  - 用户要求只处理值得做且可证明不降级的项。
- 本轮范围：
  - Parent Document Retrieval
  - 非 LLM 版 Contextual Chunking
- 风险约束：
  - 不改 benchmark 口径
  - 不改 lexical / RRF / rerank 主排序语义
  - 若 benchmark suite gated 指标回退，则本轮不放行
- 已实现：
  - `app/services/querying.py`
    - `/ask` prompt 构建前扩展 parent context
    - `/verify` 对同一 `chunk_id` 引用复用扩展上下文
  - `app/services/indexing.py`
    - embedding 输入改为 contextual text
  - `app/ingest/chunker.py`
    - 新增 contextual embedding text 构造器
  - `app/config.py`
    - 新增 `chunker.contextual_embedding_enabled`
    - 新增 `retrieval.parent_context_enabled`
    - 新增 `retrieval.parent_context_max_chunks`
- 验证结果：
  - 定向测试：`33 passed`
  - 接口测试：`9 passed`
  - `benchmark-suite.m18.json`：
    - `passed_entries=8/8`
    - `passed_gated_entries=7/7`
- 当前结论：
  - 本轮实现未触发质量门回退，可继续保留。
  - Review A / Review B 均未发现 accepted finding。
  - 允许阶段放行。

## ISSUE-A3-001 检索闭环、自适应融合与别名自动提取

- 状态：Implemented
- 背景：
  - 用户要求连续完成 `M19 失败 case 自动闭环`、`Query-type 自适应融合`、`别名自动提取` 三项工作。
  - 明确约束为：补足测试用例、质量不能回退、不能通过修改既有 benchmark / golden case / 断言口径来换取更好指标。
- 本轮范围：
  - failure record -> benchmark case draft / backlog 自动闭环
  - query type 驱动的 lexical / semantic RRF 权重
  - 基于 front matter `aliases` 的动态 alias groups
- 风险约束：
  - 不修改现有 benchmark 样本、suite gate、golden case 和断言口径
  - 不改变现有 rerank / guardrail 总流程
  - 若全量 `pytest` 或 `benchmark-suite.m18.json` 回退，则本轮不放行
- 已实现：
  - `eval/failure_closure.py`
    - 新增 failure record 读取、draft case 生成、覆盖判定与 backlog 汇总
  - `eval/run_failure_closure.py`
    - 新增 CLI，支持输出 summary JSON 与 draft JSONL
  - `eval/benchmark.py` / `eval/suite.py`
    - 为 case result 与 failure export 增加 `linked_issue_ids`
  - `app/retrieve/hybrid.py`
    - 新增 query type 自适应 fusion weights，并把权重透传到 RRF metadata
  - `app/config.py`
    - 新增 `retrieval.query_type_fusion_weights` 默认值与校验
  - `app/query_understanding.py`
    - 新增 front matter alias group 构造与动态 alias 消费入口
  - `app/services/querying.py`
    - 新增 alias group 加载/缓存
    - 对旧检索替身增加 `search_and_rerank` 签名兼容，避免无关回归
  - `app/retrieve/ranking_pipeline.py`
    - ranking pipeline 统一消费 query profile 与 alias groups
- 新增测试：
  - `tests/test_eval_failure_closure.py`
  - `tests/test_query_understanding.py`
  - `tests/test_retrieval_m2.py`
  - `tests/test_query_service.py`
  - `tests/test_eval_benchmark.py`
  - `tests/test_eval_suite.py`
  - `tests/test_health_config_scaffold.py`
- 验证结果：
  - 定向测试：`67 passed`
  - 全量回归：`128 passed`
  - `benchmark-suite.m18.json`：
    - `passed_entries=8/8`
    - `passed_gated_entries=7/7`
- 当前结论：
  - 本轮实现未引入质量回退。
  - 失败 case 闭环、query-type 融合、动态 alias 提取均已落地并受测试约束。
  - 未修改既有 benchmark 样本、golden case 或断言口径，允许阶段放行。
